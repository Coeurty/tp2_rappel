<form method="post" action="{{ path('app_reminder_delete', {'id': reminder.id}) }}" onsubmit="return confirm('Confirmez vouloir supprimer ce rappel');" class="flex justify-center">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reminder.id) }}">
	<button class="hover:bg-red-700 bg-red-800 text-white rounded-lg p-2 mt-4">Supprimer</button>
</form>
